{
  "name": "agent_generated_gns3_mcp",
  "exhibit_name": "GNS3 Network Simulator MCP",
  "type": 3,
  "command": "run-omp.sh",
  "args": [],
  "env": {},
  "description": "Complete GNS3 integration via MCP tools only. Prefer these gns3_* tools over raw REST, curl, telnet, SSH, or Python scripts against GNS3. Ensures/stops localhost gns3server, manages topologies, console (optional login), guest SSH (gns3_ssh_exec), and session cleanup (gns3_cleanup_session). Pair with the gns3-mcp agent skill for hard MCP-first rules and ask-before-cleanup.",
  "description_for_agent": "GNS3 MCP server. ALWAYS use these tools for GNS3 lab ops; NEVER reimplement via httpx/requests/curl to :3080, raw telnet/SSH, or ad-hoc Python. Tools: gns3_ensure_server (call first each session), gns3_stop_server / gns3_cleanup_session (after work: ASK user before close project or stop server), projects/nodes/links, gns3_send_console_commands (login_username/login_password), gns3_ssh_exec, images, snapshots, templates list, config templates. Resolve IDs from tool results only. Load skill gns3-mcp for playbooks and the escape ritual when a tool is missing. Prefer goal tools: gns3_prepare_lab, gns3_build_topology, gns3_configure_devices, gns3_diagnose_connectivity, gns3_run_guest_commands, gns3_prepare_image, gns3_manage_snapshot, gns3_finish_lab. Destructive goals use confirmation_token. Console responses are pure body + completed.",
  "user_params": {
    "args": {},
    "env": {
      "GNS3_SERVER_URL": {
        "required": false,
        "description": "GNS3 server URL (default: http://localhost:3080). Can be overridden per tool call."
      },
      "GNS3_USERNAME": {
        "required": false,
        "description": "GNS3 server username for REST API authentication."
      },
      "GNS3_PASSWORD": {
        "required": false,
        "description": "GNS3 server password for REST API authentication."
      },
      "GNS3_VERIFY_SSL": {
        "required": false,
        "description": "Verify TLS certificates for GNS3 REST (default true). Set false for local/self-signed."
      },
      "GNS3_SERVER_START_CMD": {
        "required": false,
        "description": "Custom command to start gns3server. If unset, default gns3server gets --host/--port from GNS3_SERVER_URL. Custom values are never rewritten."
      },
      "GNS3_SERVER_START_TIMEOUT": {
        "required": false,
        "description": "Seconds to wait for GNS3 after auto-start (default 30)."
      },
      "GNS3_SERVER_STOP_TIMEOUT": {
        "required": false,
        "description": "Seconds to wait after SIGTERM before SIGKILL when stopping localhost gns3server (default 10)."
      },
      "GNS3_SERVER_HEALTHY_CACHE_SECONDS": {
        "required": false,
        "description": "Skip re-probe window after a healthy check (default 30)."
      },
      "GNS3_CONSOLE_USER": {
        "required": false,
        "description": "Default console login username for gns3_send_console_commands (override with login_username)."
      },
      "GNS3_CONSOLE_PASSWORD": {
        "required": false,
        "description": "Default console login password for gns3_send_console_commands (override with login_password)."
      },
      "GNS3_SSH_USER": {
        "required": false,
        "description": "Default guest SSH username for gns3_ssh_exec (override with ssh_username)."
      },
      "GNS3_SSH_PASSWORD": {
        "required": false,
        "description": "Default guest SSH password for gns3_ssh_exec (override with ssh_password)."
      },
      "GNS3_SSH_HOST_KEY_POLICY": {
        "required": false,
        "description": "SSH host-key policy: accept_new (default, accept any), strict (known_hosts), warn (accept and log fingerprint)."
      }
    }
  }
}
